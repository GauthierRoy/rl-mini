# Colab (T4, CUDA 12) install.
# vLLM's default wheel is CUDA 13 since v0.20 and fails to import on
# Colab with `libcudart.so.13` (TRL imports vLLM eagerly, so even
# `use_vllm: false` configs crash). Pin to the last CUDA 12 line.
# If PEFT fails with "incompatible version of torchao", remove it
# (handled by `make install-colab`; nothing in this stack uses torchao,
# a stale preinstalled copy trips PEFT's version gate during LoRA
# injection):
#   pip uninstall -y torchao
-r requirements.txt
vllm<0.20
